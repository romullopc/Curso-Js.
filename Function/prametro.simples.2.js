
function soma(n1=0, n2=0) {     // atribui valor '0' mesmo que não seja dito pelo usuário, evitando mensagem de erro 'NaN';

    return  n1 * n2         // esta maneira também retorna o valor de expressão matemática;

}   

console.log(soma(5));       //forma resumida de retornar a função com parâmetron ão precisando de declarar uma variável.